/*
$('#nominationForm').on('beforeSubmit',function (){
    let form = $(this);
    $.post(
        form.attr('action'),
        form.serialize()
    ).done(function (result) {
       if(result.message=="Success"){
           $(document).find()
       }
    });
});*/
